#Merging Branches:

1-->git checkout master
2--> # to find the difference between branches
   git diff branchname
3-->git status
4--> git push --set -upstream origin branchname
     git push
5--> #for easier upstream
     git -u origin branchname
     
#STEPS TO DELETE FEATURE BRANCH AFTER MERGING
 1-->git branch
 2-->git branch -d branchname
 
 #undo 
 1-->git reset filename(to remove commit)
 
 2-->git reset HEAD ~1
 #HEAD- POINTER TO LAST COMMIT
 
